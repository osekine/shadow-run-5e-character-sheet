import 'package:flutter/material.dart';

class BigText extends StatelessWidget {
  final String _text;
  final Color _color;
  const BigText({super.key, required String text, required Color color}) : _text = text, _color = color;

  @override
  Widget build(BuildContext context) {
    return ;
  }
}